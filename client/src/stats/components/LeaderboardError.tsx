import type { Component } from "solid-js";

export const LeaderboardError: Component = () => {
    return (
        <div class="leaderboard-error">
            <h2>Unable to load, please try again.</h2>
        </div>
    );
};
